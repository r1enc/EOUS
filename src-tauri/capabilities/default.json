{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the EOUS desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "sql:default"]
}
